<div class="container">
<h1>Editing item</h1>

<%= form_for @item, url: item_path(@item), method: :patch, html: { multipart: true } do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Back', items_path %>
</div>