<div class="container">
    <div class="page-header">
        <h1>New Item</h1>
    </div>
<%= form_for @item, url: items_path, html: { multipart: true } do |f| %>
  <%= render 'form', f: f %>
<% end %>
<%= link_to 'Back', items_path %>
</div>