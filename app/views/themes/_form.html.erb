  <% if @theme.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(@theme.errors.count, "error") %> prohibited
      this theme from being saved:</h2>
    <ul>
    <% @theme.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
  <% end %>
  <% if !@item.nil? %>
    <%= f.hidden_field :item_id, :value => item.id %>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :about %><br>
    <%= f.text_area :about %>
  </p>

  <p>
    <%= f.submit %>
  </p>
